// Header.js

import React from 'react';
import CustomNavbar from './Navbar';

function Header() {
    return (
        <header>
            <CustomNavbar />
        </header>
    );
}

export default Header;
